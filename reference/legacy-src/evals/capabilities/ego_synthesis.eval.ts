import { describe, it } from "vitest";

describe("Legacy Ego Synthesis Eval", () => {
  it("is covered by capabilities/inner_synthesis.eval.ts", () => {
    // Kept as a compatibility marker for older eval dashboards.
  });
});
